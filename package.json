{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@neumaennl/blerf": "0.0.11"
  },
  "dependencies": {},
  "scripts": {
    "build": "blerf build",
    "test": "blerf build && blerf test",
    "pack": "blerf build && blerf pack:publish",
    "deploy": "blerf build && blerf pack:deploy"
  }
}
